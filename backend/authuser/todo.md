# TODO

- [ ] I need to modify encx so that I can expose the serializer and change the userIDBytes definiton in revoke_all_user_sessions service function (in session) to use that serializer
instead of the json one (that is the default one for now). That implementation is not stable


